# <Tên function> — Feature Summary  (KNOWLEDGE mẫu)

> **Knowledge = AI DỰA TRÊN TRI THỨC GÌ.** File này là tri thức nền per-feature (dữ kiện của 1 tính năng).
> KHÔNG sửa file template này. KHÔNG chứa các bước thực thi (skill) hay danh tính (role).
>
> Ghi chú: Knowledge có 2 loại — (a) **per-feature** như file này; (b) **technique/registry** ổn định
> (vd `viewpoint-library.md`, `06W-missing-rule-technique.md`). Cả hai đều nằm trong `knowledge/`.

## 1. FEATURE OVERVIEW
_[1–2 câu]_

## 2. ACTOR & USER ROLE
_[ai dùng]_

## 3. BUSINESS RULES
1. _[rule 1]_  `(BR-01)`
2. _[rule 2]_  `(BR-02)`

## 4. HAPPY PATH
_[step-by-step]_

## 5. ALTERNATE FLOWS
_[luồng phụ / exception]_

## 6. OUT OF SCOPE
_[không đề cập]_

## 7. OPEN QUESTIONS
1. _[cần hỏi BA]_
   - → `[GIẢ ĐỊNH]` _[trả lời tạm nếu cần chạy tiếp — chờ BA xác nhận]_

---

## Missing Rule Report (6W)
> Trạng thái mặc định `New`. Người duyệt chuyển: `Confirmed` / `TREO` / `Rejected`.
> 6W = What if input lạ / state lạ / data lạ / What when timing / Who else actor / What happens after.

| ID | Mô tả gap | Loại (6W) | Requirement hiện tại | Rủi ro | Câu hỏi cho BA | Trạng thái |
|----|-----------|-----------|----------------------|--------|----------------|-----------|
| G1 |  |  |  |  |  | New |

---

## Traceability
> Mọi rule/gap ở trên phải trace được ngược về file nguồn trong bộ tài liệu đầu vào.

| Item | Nguồn (file / mục) |
|------|--------------------|
| BR-01 |  |
| G1 |  |